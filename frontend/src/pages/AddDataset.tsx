const AddDataset = () => {
	// addDataset drop down menu type thing  -> user can choose its id, upload a file
	// validate ID and stuff and when dataset is added, user can see it added and have it refresh dynamically

	return <>
		<h1> this is the add dataset page</h1>
	</>

}

export default AddDataset;
